# 晚间来源与设备验证（2026-09-22）

用户已取消14点停止限制，继续原计划；不做整集测试。

## 同一候选包的17源完整短测

电视：小米 Android9 / WebView66。APK SHA256 `f9786d4fe4edb58854c63cbcd72dcb6952aa408a6dfe33feb0579487f2b2f114`。固定规则仍为 KazumiRules `0d85fc80ab6c208548d9ee9c9e81271b08ff7f39`，17条完整检查；没有修改电视规则、启用/隐藏状态或用户历史。

**9个来源、17条线路短测通过**。每条通过要求首帧、进度推进、拖动（可用时）、暂停和继续，不代表整集或全站节目通过。失败不自动归因于WebView。

|来源|通过线路（从1开始）|未通过或未进入播放|
|---|---|---|
|7sefun|2|线路1 resolve：HTTP 404|
|aafun|—|线路1 resolve：HTTP 400|
|AGE|—|chapters：SocketTimeoutException|
|akianime|1|线路2 resolve：MediaResolutionFailure|
|baimao|1、2、3、4、5|线路6 resolve：MediaResolutionFailure|
|dalvdm|—|search：需人工验证|
|DM84|—|search：HTTP 522|
|ezdmw|1、2|线路3 resolve：MediaResolutionFailure|
|giriGiriLove|1、2|本轮所选线路通过，仍有节目范围限制|
|gugu3|1|本轮所选线路通过，仍有节目范围限制|
|LMM|—|search：需人工验证|
|mgnacg|—|search：需人工验证|
|moonci|2、3|线路1 resolve：HTTP 400|
|mutefun|—|search：需人工验证|
|MXdm|1、2|本轮所选线路通过，仍有节目范围限制|
|sorani|1|本轮所选线路通过，仍有节目范围限制|
|xfdmneo|—|三个关键词均空结果|

## 真实验证码及现代分支

- giriGiriLove：用户读取验证码，实际原生提交按钮提交后，原搜索恢复4条结果；此前 `1de90384…` 包紧接第12集两线短测通过。上表 `f9786d4f…` 再次通过两线。验证码Cookie自然保留，没有人工伪造成功或跳过服务端。
- dalvdm：旧电视与现代WebView实际页面均加载jQuery/MAC.Verify函数。脚本显示在输入获得焦点后才插入验证码图；电视实际点击网页输入框使图片数0→1。固定规则使用旧DS模板XPath，与实际MAC控件不符。诊断等待超时不记验证通过；兼容实现与真实正确输入恢复仍在推进。
- xfdmneo：已保存响应重定向到综合门户且旧搜索列表不存在；AGE实际详情网络超时，尚未进入集表解析。不能据此把它们归为已修复或播放器故障。
- 现代TV模拟器 API36 / WebView143.0.7499.24：通用发现测试通过，含document-start early XHR、跨域iframe、兼容分支、挑战优先级和双页面释放。是受控能力测试，不代替真实来源播放。
- 历史列表详情返回焦点修复，在电视和现代TV均通过真实遥控Back/确认回归，重新确认打开详情而不触发续播。
- 现代验证受控按钮/异步/POST-cookie测试通过；62秒等待、HOME暂停轮询与同一WebView恢复通过；首次清空输入状态检查失败，实际disabled外观与缓存节点enabled冲突；保留失败日志。测试改为等待JS回调、检查实际按钮并在API33+清除可访问性节点缓存后，同样62秒及HOME全路径通过，明确DOM长度4→0与Button enabled→disabled，没有改生产按钮状态或放宽断言。

原始日志、私有页面及截图仅在忽略目录 `artifacts/evening-sources-20260922`。没有上传APK或私有凭证。

## 标准MacCMS控件兼容增量

候选APK SHA256 `1927755418a9f8b07bface1f00146cba58037925c5d69abf121c3a6525d1a4b7`，晚于上述全目录轮。仅在启用图片验证、配置三个控件均未命中、真实MAC.Verify接口存在且标准输入和提交控件唯一时发现现有控件；规则配置优先，图片仅同源。复用站点原有focus与click处理，不生成图片路径、验证接口或成功结果。可见/无窗口WebView聚焦事件只补发一次。

- 190项单测、release/testAPK构建及lint通过。
- 电视WebView66及现代WebView143均通过8组控件回归：正常焦点生成图/人工供码及网页填码/原处理器清空、重复输入、重复按钮、非MAC、配置优先、跨域图片拒绝、未启用、错误类型。含不伪造验证成功断言。
- 旧电视既有验证类型、错误成功防护、完整POST/Cookie回归通过；现代62秒等待、HOME恢复及布局/提交回归通过。
- dalvdm实际电视页面已无需手动点击而显示原生验证码图，正确人工输入后的恢复尚待验证。此前diagnostic超时均作为未完成人工验证，不记成验证码错误。
- mutefun新页面未收到人工输入，5分钟等待结束，仍属本轮未完成验证。
- aafun现代WebView也返回媒体HTTP400，不把此线路失败简单归因于旧内核。

## 换源后再次入播与电源键增量

同一 `19277554…` 生产APK，新增测试参数 `verifyReentry` / `verifyStandby`，测试APK构建通过。日志位于本地 `artifacts/recovery-20260922/mxdm-reentry-standby.txt`。

- 真实baimao→MXdm第12集：取消仍暂停在10秒；32秒换源，目标首次记录37543ms并继续至42秒；返回目标线路1第12集通过。
- 再次点击当前集，从已保存42733ms继续至47秒，证明本次跨源返回后再入播读取目标历史；不是完整历史入口覆盖。
- 电源键检查未通过：电视厂商 `PowerManagerService` 明确记录应用不在待机白名单，随后 `forceStopPackage` 杀死应用及进程内测试。不能把 instrumentation 的 `Process crashed` 简单当作应用异常，也不能声明暂停/唤醒恢复通过。需要主机侧跨进程检查真实重新进入及历史恢复，不修改系统白名单。
- 测试进程死亡导致 finally 未执行；已单独使用本轮 `source-switch-1790085208716` 检查点恢复三份用户存储，工具确认 `restored_and_equal stores=3`。已唤醒电视，确认 Awake，重新打开应用。没有使用旧检查点覆盖本轮用户数据。

随后改用主机侧操作正常应用入口（无进程内测试宿主）：先创建新检查点 `host-power-20260922a`，从主页最近观看进入番剧详情，再明确选择“继续第13集”。真实baimao视频从29秒推进，暂停于44秒后按电源键；电视为Asleep且原PID3049消失。唤醒并重新启动后PID4668，从最近观看进入同一番剧仍显示“第13集 · 0分44秒”；明确选择继续后视频渲染并推进至50秒，弹幕显示6167条已自动匹配。截图与UI证据保存在本地 `artifacts/recovery-20260922`。结束后恢复新检查点，三份存储核对一致。这证明本机系统强停后的手动重开/历史续播，不证明自动回到播放页、播放中突发断电、深度待机或其他系统组合。

## 无内置凭证候选与来源候选

当前生产代码无内置凭证构建SHA256 `f5c08fcffb2a43d4473dc2603ed78f79cf6c41f9043c9213b4153bd5fea2d6b1`，仍为code49 / 0.3.3-preview.3；发布尚未执行。release/test构建、单测及lint通过，生成BuildConfig两凭证字段为空。旧电视WebView66与现代TV均通过 `credential-free-release` 专项：正式包、隔离设置UI保存/读回/移除固定测试凭证、不回显密钥、无凭证仍渲染本地视频并推进3秒、播放器配置指引。没有将测试值发送到远端，此证据不等于有效凭证鉴权通过。

xfdmneo另建[独立候选规则及门槛](source-candidates/xfdmneo-20260922.md)：公开旧版入口仍存在，实际搜索要求DS图片验证。保留固定17源快照，候选尚未导入设备或通过人工验证、集表及播放，不计入已通过来源。

无凭证候选随后在旧电视完成MXdm第12集两线路真实短测，两线均通过首帧、推进、拖动及暂停/继续，记录 `public-mxdm-tv.txt`。不将此前全17源结果改标为本候选重测。结束后电视恢复原 `19277554…` 本地APK并重新打开，保留原有弹幕配置体验；模拟器保留无凭证候选。此轮尚未上传Release。
