# 播放网络异常恢复阶段 · 2026-09-23 至 24

状态：现代模拟器的本阶段 A–G 检查已完成；旧电视通过三项短时受控回归，物理断网恢复仍待可保证恢复的控制链路。没有提交、推送或发布，也没有整集播放。构建、受控回归和模拟器网络限制不能代替旧电视物理断网验收。

## 最终候选与验证

- 生产 APK SHA-256：`9EFBBEFCC5F2F42345F6F5CA4ED7B371A941621F1405828EF10FA0E1592E8FED`；最终测试 APK SHA-256：`2EE233D38C6110F712C696311AF54D3E4C973CB1D2B96B9594C4BC4776D20B96`。
- WebView 主机查找错误 -2 后，一次解析操作内最多顺序重试 5 次，恢复循环最长 25 秒且可取消。25 秒从首次 -2 后开始，不包括此前的 HttpText、网页或媒体解析。非 -2 错误保持自身分类。诊断事件只记录安全计数。
- 首次解析前置请求失败且 WebView 主页面未完成时，超时提示为中性“网页与媒体发现”表述；若主页面已完成但没有媒体，仍归为媒体发现失败。两种分支均有单测反例。HOME 时取消在途解析，返回后提示用户明确重新解析。
- `:app:testReleaseUnitTest :app:assembleRelease :app:assembleDebugAndroidTest :app:lintRelease` 成功；55 个 XML 报告合计 225 项单测，失败、错误、跳过均为 0。最终生产包在 `emulator-5560` 上的 S0、播放恢复、会话恢复、过期媒体、HOME、验证流程和目录验证 7 项受控回归均通过。

## A–G 实测矩阵

| 场景 | 本阶段观察 | 边界 |
| --- | --- | --- |
| A 首次解析断网 | baimao 最终候选真源在受限网络下收到 WebView -6，界面明确显示连接失败；网络恢复后首次手动重新解析同集并播放推进 5 秒。此前候选的前置请求超时路径也曾以中性提示并恢复通过；旧版误报“候选 0”的失败记录保留。 | -6 不是 -2，不计入 -2 重试策略；最终候选本轮未再次命中中性超时分支。 |
| B 播放后 IO 错误 | baimao 与 AGE 真源均触发播放器 2001；恢复为同集、新播放器、原进度及暂停意图，明确播放后推进 5 秒。AGE 最终轮受限期主机查找重试 5 次、恢复后 4 次。 | 额外诊断 DNS/HTTP 探测为 0；正常解析流程仍产生网络请求。 |
| C 重新获取媒体 | B 的恢复创建新播放器；最终 `playback-expired` 受控回归通过。 | 首次过期媒体 fixture 曾崩溃，后续独立重跑及最终轮通过；未覆盖所有真实媒体过期方式。 |
| D 再次断网与连点 | baimao 同一播放会话出现两次真实 2001，均恢复同集、进度和播放推进。第二次快速点击同一重试控件，两次点击均被接受，但只完成一次解析；第二次恢复的主机查找重试 4 次，不超过每次 -2 后 5 次。 | 全轮 `restored_host_retries=13` 是各恢复过程累计，不是一次点击的次数；25 秒仅约束 -2 恢复循环。 |
| E 返回退出 | 真实 2001 后在受限网络下启动重试，立即返回退出；网络恢复后观察 45 秒，无迟到解析成功、无后台播放器。换源另有 S0 受控回归。 | 恢复中真实换集、换源没有分别运行。 |
| F HOME 与进程 | 真实 2001 后在途重试时 HOME；恢复网络并观察 45 秒，无后台解析成功或播放器；返回显示明确重新解析提示。正常 LEANBACK 启动后的 HOME、`am kill`、再次 LEANBACK 进入得到新 PID 和初始设置页，无自动播放。 | 进程死亡专项是在正常入口，不是在真实网络重试进行中。先前 HOME 轮曾出现后台成功事件，修复后最终轮通过；保留失败证据。 |
| G 验证保护 | 最终 `verification` 精确 POST/Cookie、假成功防护、取消与 `catalogue-verification` HOME 后无自动播放通过。 | 真实验证码需人工协作，受控测试不等于真实验证码验收。 |

AGE 首轮失败在测试脚本季名匹配（页面标题用“Ⅲ”），没有进入网络受限阶段。修正后两轮真实 2001 恢复通过，不能将脚本失败算作产品恢复失败。最终轮记录在忽略目录 `artifacts/network-stage-20260923/age-final2`。

## 旧电视与关屏交还

旧电视 `192.168.123.217:5555` 为 MiTV-ASTP0、API 28。用户退出电视设置界面后，临时安装最终候选，在亮屏下的 `s0-regression`、`playback-recovery`、`playback-background` 三项短时受控回归均通过。此前两项受控回归曾因 Xiaomi 设置窗口占焦点失败，不能作为产品失败判定；本轮重新运行通过。未运行整集播放，也未向电视注入物理断网：无线 ADB 与电视网络共用链路，断网会失去恢复控制。

测试前、候选测试后及装回原版后，三项用户数据检查点 `stage-network-old-tv-20260923-before` 均读回未变。原版主 APK 与测试 APK 均已重新安装；电视设备侧 SHA-256 分别为 `26AE4F1E06AB978A2F4DC459051060ECC640ED89655438DDACFC18AD21252E09`、`24C60C9B9E17A6B5DB1FC75D7D7848E93E923E63B50FA9C5B2BCB00FAD9C5712`，与本地保全件一致。用户准备睡觉后发送 POWER，设备读回 `mWakefulness=Asleep`、显示 `OFF`；此后没有再唤醒电视。关屏期间不进行依赖焦点、首帧或播放画面的验收。

## 保全与证据位置

所有现代模拟器操作使用 `adb -s emulator-5560`；测试检查点恢复并比较，外层数据标签 `stage-network-20260924-o` 至 `t` 等读回未变。网络控制脚本每轮 `finally` 恢复 full/none，主机记录 `network_finally_restored`，模拟器读回无限速及 0 ms。没有清除 Cookie、全局代理或应用数据。

原始记录留在忽略目录 `artifacts/network-stage-20260923`，主要包括 `baimao-initial-final-2`、`baimao-double-final`、`baimao-back-final`、`baimao-home-final`、`age-final2`、`final2-*.txt`、`old-tv-*-final.txt` 及私有截图。早期失败（包括过期媒体 fixture 崩溃、旧分类错误与 HOME 后台成功）继续保留，不以最终通过覆盖。原始页面、来源规则、截图和凭证不得纳入 Git。
