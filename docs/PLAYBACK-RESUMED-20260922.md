# 2026-09-22 恢复推进记录

用户明确取消原 14:00 停止限制，继续原生播放优先迁移。旧一次性停止自动任务已删除；不做整集测试，保留主页方向、用户数据及其他任务文件。

## MXdm 换源搜索

恢复后原包再次复现：预查询短词有结果，实际换源短词为零。为同一 RuleRepository 实例增加默认关闭的响应观察回调，仅由测试显式开启保存实际响应到本地诊断目录，不增加请求，不记录 Cookie。

实际证据：完整片名请求为正常零结果页；紧接的正确短词请求 HTTP 200，返回 `.jump > .tit` 系统提示、独立问候行及“请不要频繁操作，搜索时间间隔为3秒”。原检测仅支持 `.msg-jump`，漏掉提示导致 XPath 搜索错误呈现零结果。不是已证实的 XPath 并发问题。

修复：通用提示检测支持该紧凑结构，仍要求明确系统提示标题和整段限频文本。复用同源串行、3秒加250ms等待及每次页面最多一次重试；再次限频明确报错。验证页脚本同步识别，限频不视为验证码通过。

## 已看番剧入口

详情页继续按钮仍使用最新记录；查看原来源选集独立使用最新在线记录，避免较新的缓存观看隐藏已有在线目录。新增在线第12集与较新缓存第20集混合记录回归，验证各入口使用正确记录、目录不自动播放和返回焦点。

## 构建与证据边界

- 本地构建 SHA256：`1de90384ba66b928df864d5bb9b21b8292fa53c4a767eb8a98e12921491347d1`，仍为 code49 的未发布增量诊断候选。
- `build-throttle.txt`：release、instrumentation APK、lint 通过；190项单测，零失败或错误。
- `mxdm-cross-initial.txt`：旧行为重复失败，三份用户存储恢复相等。
- `mxdm-cross-diagnostic.txt`：初始暂停阶段超时，未到目标换源，不能判定 MXdm；数据恢复相等。
- `mxdm-cross-diagnostic-retry.txt`：实际响应定位限频漏识别，三份用户存储恢复相等。
- `mxdm-cross-fixed.txt`：修复后同一实际 UI 请求先遇限频、等待重试后获得11条结果；真实白猫→MXdm第12集从32秒切换，目标首次记录37631ms并推进至42秒；取消保10秒暂停、返回MXdm线路1当前第12集均通过，三份用户存储恢复相等。这是短测，不代表整集或全来源通过。

`recent-watch-fixed.txt`：电视混合在线/缓存历史回归通过，含继续最新缓存、查看已有在线原来源目录、不自动播放及返回焦点。该导航测试使用隔离的历史样本，不是新增真实播放证据。

`verification-throttle.txt`：旧电视 WebView 受控验证回归通过，新增动作回调已完成但随后出现紧凑限频提示时仍不得判定验证成功；保留按钮、异步脚本、图片输入提交和完整POST/Cookie恢复等已有检查。不是现实站点正确验证码验收。

本批结束时 `user-data-restored.txt` 确认恢复开始时的三份存储并逐项相等，重新启动正常主页。未清除应用数据、Cookie或改动主页方向。

原始证据在忽略的 `artifacts/resumed-20260922`。开始前新增 `resumed-20260922` 用户数据检查点，不使用午间旧检查点覆盖其后用户操作。真实 giri 正确验证码恢复、完整新包全来源/线路验证、现代 WebView 分支等仍待验证。公开测试版仍未发布；不得上传未获明确授权公开的私有凭证。
