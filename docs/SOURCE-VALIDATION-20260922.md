# 全目录来源短时抽测（2026-09-22）

固定 KazumiRules `0d85fc80ab6c208548d9ee9c9e81271b08ff7f39`，共17条；电视来源已备份，17/17安装且与固定规则一致，无需补装，启用/隐藏状态保留。设备为小米 Android 9 / WebView 66.0.3359.158。

本轮 `full-47-initial.txt` 完整检查17条来源，**7条来源至少一条线路通过，共14条线路短测通过**。短测要求首帧、实际进度推进、拖动、暂停和继续；每条线路仅抽一个节目/集数，不代表全站。用户已取消整集测试，本轮未等待整集播放。

此全目录轮使用本日较早的code47候选，晚于MXdm静态提取初修，早于提取器边界收紧和导航Parcel修复。最终包的定向复测另列，不能把两次构建混称同一轮结果。

当时code47的最终APK定向复测 `MXdm-47-final.txt`：第12集两条线路均通过全部短测步骤。另有正常首页入口baimao第13集播放、系统杀进程后暂停恢复及显式继续的实际画面证据，见[执行记录](PLAYBACK-EXECUTION-20260922.md)；该操作验收不额外计入上方14条线路统计。

| 来源 | 本次实际集数与线路（从1开始） | 结果 |
|---|---|---|
| 7sefun | 第12集，2线 | 线1媒体HTTP404；线2短测通过 |
| aafun | 第12集，1线 | 媒体HTTP400 |
| AGE | 未进入选集 | 集表请求失败，未提取到HTTP状态码 |
| akianime | 两线仅7/8集，均回退01 | 线1通过；线2媒体发现失败 |
| baimao | 线1–5第12集；线6仅2集，回退第1集 | 线1–5通过；线6媒体发现失败 |
| dalvdm | 未进入选集 | 搜索需要人工验证；另查验证DOM无图片，结构与固定XPath不符 |
| DM84 | 未进入选集 | 搜索HTTP522 |
| ezdmw | 三线均抽标题“12” | 线1–2通过；线3媒体探测失败 |
| giriGiriLove | 未进入选集 | 搜索需要人工验证 |
| gugu3 | 仅11集，回退第01集 | 媒体发现失败 |
| LMM | 未进入选集 | 搜索需要验证，本轮未关闭验证流程 |
| mgnacg | 未进入选集 | 搜索需要人工验证 |
| moonci | 第12集，3线 | 线1媒体HTTP400；线2–3通过 |
| mutefun | 未进入选集 | 搜索需要人工验证 |
| MXdm | 第12集，2线 | 两线通过；原公开版同日两线媒体发现失败 |
| sorani | 第12集，1线 | 短测通过 |
| xfdmneo | 未进入选集 | 无职转生、海贼王、柯南搜索均为空 |

不同日期站点结果变化，不覆盖[前轮记录](SOURCE-VALIDATION-20260920.md)。尤其gugu3、7sefun线1本轮失败，不能沿用旧通过；LMM/mutefun过去验证成功也不能标成本轮已经通过。媒体HTTP错误、搜索验证、集表失败、播放器发现错误分别记录，不统一归因于WebView。

原始逐事件记录、页面与用户备份仅保存在本地忽略目录 `artifacts/source-audit-20260922`；公共文档不包含Cookie、签名播放URL、配置快照。

## 午间code49增量与环境边界

较早code49候选（APK `4a1eaca0…`）在真实gugu3仅11集目录回退第01集，解析、首帧、实际推进、拖动和暂停继续全部通过（`artifacts/midday-20260922/gugu3-49.txt`）。本次通用修复保留原嵌入页面，候选iframe单独探测，不再用顶层候选导航覆盖原页面。只计该节目/线路样本，不将旧轮7源14线统计改写为同一构建全源结果。

该候选的17源复测`full49.txt`中途发现电视处于休眠、屏幕关闭，首帧超时受环境影响，随后停止；没有完成有效的全目录矩阵。这些超时不计来源故障或版本回退。测试增加播放窗口保屏及交互状态检查，休眠时取消整轮，未更改电视电源设置。唤醒后候选及最终APK定向结果另列，详见[午间记录](PLAYBACK-MIDDAY-20260922.md)。

giriGiriLove本轮重新打开真实验证页，但5分钟窗口内没有收到人工验证码，正确输入后的原搜索恢复仍未验证。受控页的长等待、后台返回和固定测试输入恢复不替代真实站点验收。

醒屏后在`2bb10411…`候选（晚于验证错误优先级修复，早于随后发现的播放器焦点修复）完成三个来源定向复测：

| 来源 | 抽样 | 实际结果 |
|---|---|---|
| gugu3 | 单线第01集（目录仅11集） | 解析、首帧、推进、拖动、暂停继续全部通过 |
| akianime | 线1–2均回退01 | 线1全部短测通过；线2仍媒体发现失败 |
| baimao | 线1–5第12集；线6第1集 | 线1–5全部短测通过；线6仍媒体探测失败 |

对应本地`gugu3-49-awake-final.txt`、`akianime-49-awake-final.txt`、`baimao-49-awake-final.txt`；文件名中的final是当时的候选名，不代表后续焦点修复APK。同日早期全目录统计保持原样，不合并不同构建为完整新矩阵。

焦点修复后的`f62680d3…`候选在跨源UI中再次查询MXdm：预查询能找到第三季，但正式UI提交相同关键词返回0条（日志确认`expected_keyword=true`）。该轮只完成baimao实际播放和取消换源保持暂停，未进入MXdm续播。差异原因待查；不能将早期MXdm播放通过扩大为当前跨源UI已经通过。

同一最终候选真实baimao→sorani第12集跨源样本通过：32秒切换，目标首记录37941ms并推进至43秒；取消保持10秒暂停，返回当前集高亮且目标来源历史正确。用户数据检查点恢复相等。此结果不覆盖MXdm的空结果问题，也不代表两个来源所有节目或不同片头时长均已对齐。

最终`f62680d3…` APK另以`gugu3-49-focus-final.txt`复查第01集，解析、首帧、推进、拖动和暂停继续全部通过。原规则和目录完整矩阵的构建边界保持上述区分。

## 恢复推进后的 MXdm 增量

`1de90384…`本地候选通过真实白猫→MXdm第12集短测：限频提示后有界等待重试得到11条结果，32秒切换、目标首次记录37631ms并推进至42秒，取消保暂停、返回线路1当前第12集及三份存储恢复相等。原空结果来自未识别的HTTP200限频提示结构，修复不依赖来源名。不是全目录同包重测，也不是整集验收。详见[恢复推进记录](PLAYBACK-RESUMED-20260922.md)。

## 晚间完整目录轮

`f9786d4f…`候选已完成同包17源检查，9个来源的17条线路通过短测。giriGiriLove由用户输入验证码后实际恢复搜索，再完成第12集两线路短测；gugu3也通过。逐来源表、现代WebView分支和残留项见[晚间来源与设备验证](PLAYBACK-EVENING-20260922.md)。此前code47轮的7源14线仍保留为历史证据，不与本轮混算。

2026-09-23 增量（不同构建，不能拼成原整表同包通过）：AGE固定规则第12集5线，在本地候选 `5d4c87c29455303b35a280743fe3a783d0b75ad12658026c5846e67935dba95c` 的WebView66真机与WebView143模拟器均完成解析/首帧/推进/拖动/暂停继续。旧核1线计算配置、4线字面量变量关联修复，详见[兼容记录](INLINE-PLAYER-METADATA-20260923.md)。其他来源仍以各自构建的已有记录为准，未重写为本候选全通过。

## 2026-09-23 AGE修复后的剩余线路复核

候选 SHA256 `5d4c87c29455303b35a280743fe3a783d0b75ad12658026c5846e67935dba95c`（与上述AGE、MXdm回归相同，早于播放诊断入口改动）。固定规则未改动；仅短播放，不做整集测试。

|来源|设备|当前结果|
|---|---|---|
|akianime|MiTV WebView66、现代WebView143|第1线短测通过；第2线均媒体发现失败。目录无12集，回退01集；不能归为仅旧内核问题。|
|ezdmw|MiTV WebView66|第1、2线短测通过；第3线媒体探测失败，所观察候选响应不是可识别媒体。|
|baimao|现代WebView143|第1–5线短测通过；第6线已发现媒体候选，但探测连接失败或超时。未认定站点永久不可用。|

证据：忽略目录 `artifacts/recovery-20260922/inline-akianime-tv.txt`、`inline-akianime-modern.txt`、`residual-ezdmw-tv.txt`、`residual-baimao-modern.txt`，均已终止并保留失败行。

akianime第2线追加只读页面诊断：搜索HTTP200/10条，集表2线，节目页HTTP200；静态媒体0、静态iframe0，运行时播放器由公开脚本生成。拟进一步匿名请求第三方解析页时，自动审批因页面参数的敏感性与发送授权尚未明确而拒绝；没有重试或间接绕过。因此仍列“失败原因未定”，不能以此确认站点故障或宣布解析已修复。原始页面、参数及脚本仅保存本地忽略目录，不公开。
