# 全目录来源验证（2026-09-20／21）

目录固定为 KazumiRules `0d85fc80ab6c208548d9ee9c9e81271b08ff7f39` 的17条正式条目。电视已备份并补齐LMM、xfdmneo；没有把目录外历史JSON作为当前推荐来源批量导入。

## 结果口径

**物理电视最终这一轮：17条全部有结果，5条来源至少一条线路短播放通过，共7条线路通过。** 通过来源为7sefun、akianime、gugu3、moonci、sorani；不是“17源都能播放”。随后独立LMM电视验证补测新增1源2线，因此**含明确标注补测的累计电视证据为6源9线**，不改写全目录原轮统计。短播放包含实际首帧、进度推进、快进、暂停和继续，只证明当次抽测的作品/线路。

- 物理电视：小米Android9／WebView66.0.3359.158，证据 `full-tv-final-device.txt`。
- 现代内核：独立API36模拟器／WebView143.0.7499.24，证据 `full-modern-v3-device.txt`。该轮6条来源、12条线路短播放通过。额外 `LMM-modern-verified.txt` 为验证后的独立复测，新增LMM的2条成功线路；不能将额外结果无标识混入全目录那一轮。
- 两轮不同时，站点剧集和线路内容会变化；现代第三轮早于最后一批请求头等修改，不能据此做严格的仅WebView版本变量对照。模拟器通过也不代替电视解码与性能证据。
- 表中线路从1开始。集数以事件记录中的实际标题为准，不完全依赖脚本的 `fallbackEpisode` 字段。

## 17条来源矩阵

抽样作品均优先搜索“无职转生”，能选到第三季时使用第三季；找不到12集时有明确替代。尚停在搜索／验证阶段的来源不能写成已经抽测了某集。

| 来源 | 物理电视最终轮：实际抽样与结果 | 现代内核第三轮／明确标注的补测 | 分类及下一步 |
|---|---|---|---|
| 7sefun | 第12集，线路1、2短播放通过 | 搜索超时，未进入选集 | 本轮电视成功；网络时变，不能沿用旧轮线路1的404覆盖最终成功 |
| aafun | 第12集，唯一线路媒体HTTP400 | 第12集，唯一线路同为HTTP400 | 裸`//`集表解析已修；继续核对媒体请求/服务响应，不能说客户端播放已修 |
| AGE | 搜索有5项，集表请求超时，未进入播放 | 同阶段超时 | 网络/详情访问阻断；需同条件原版对照 |
| akianime | 两线只列7／8集，均回退01；线路1短播放通过，线路2未发现媒体 | 同样回退01；线路1网页HTTP500，线路2未发现媒体 | 部分可用且时变；最终电视成功否定“旧WebView一定不能播放该源” |
| baimao | 线路1–5抽第12集，线路6仅2集而回退第1集；六线均未发现媒体 | 线路1–4第12集、线路5回退第1集均短播放通过；线路6第1集媒体探测失败 | 设备/环境差异待定位。**线路5两轮不是同一集**；不能把全部差异直接归因于WebView66 |
| dalvdm | 搜索需要人工验证，未选集 | 可见验证页显示安全验证，但没有生成验证码图片；固定规则XPath与该页面结构不符 | 验证页脚本/上游规则还需修订，未通过 |
| DM84 | 搜索HTTP522，未选集 | 同为HTTP522 | 站点/链路响应失败；没有客户端修复成功证据 |
| ezdmw | 三线均列13集，抽标题“12”；线路1、2未发现媒体，线路3媒体探测失败 | 同样抽12；线路1、2短播放通过，线路3媒体探测失败 | 兄弟轴/union集表遗漏已修；媒体发现差异仍待同条件定位 |
| giriGiriLove | 搜索正确识别需验证，未选集 | 全目录轮需验证；后续人工图片验证通过并恢复4项搜索，线路1第12集、线路2回退01均短播通过 | HTTP验证码页与随后WebView限频提示不同；已补严格限频识别；现代环境已有验证与播放闭环，电视仍待验 |
| gugu3 | 唯一线路只列11集，回退第01集，短播放通过 | 同样第01集短播放通过 | 本轮有播放证据，**没有第12集或全剧可用证据** |
| LMM | 全目录轮搜索验证未完成。**后续电视补测**：自动验证后恢复5项搜索；第12集线路2、3短播通过，线路1媒体404 | 全目录轮需验证。**额外补测**：可见网页自动验证后搜索恢复5项；第12集线路2、3短播放通过，线路1媒体404 | 新旧设备均有验证与抽样播放闭环，线路1仍失败 |
| mgnacg | 全目录轮需验证，未选集 | 后续图片人工验证通过、恢复4条搜索；第12话线路2/3/4短播通过，1/5失败 | 已有现代环境验证及播放闭环；早期空白页未复现，电视仍待该源验证 |
| moonci | 线路2、3第12集短播放通过；线路1实际选中**第13集**，媒体400 | 三线均第12集；线路2、3短播放通过，线路1媒体400 | 部分可用。电视线路1索引回退被脚本标成false，不能称两轮都测试了第12集 |
| mutefun | 后续图片人工验证成功、恢复5条搜索；第12集线路1短播通过、线路2失败 | 图片人工验证后原搜索恢复5项 | 电视已有人工验证与抽样播放闭环，未声称自动识别图片 |
| MXdm | 唯一线路第12集未发现媒体 | 同样第12集短播放通过 | 设备/环境差异待定位，不能仅凭统计JS报错确证根因 |
| sorani | 唯一线路第12集短播放通过 | 同样第12集短播放通过 | 另有电视第01集完整观看及自然下一集证据，见下节 |
| xfdmneo | 无职转生／海贼王／柯南三次均空 | 同样三次均空 | 实际页面重定向至综合门户；固定规则与站点不一致，未猜测替换域名 |

## 完整观看证据与范围

`real-watch-tv.txt` 和 `real-watch-result/result.txt` 明确记录：电视通过生产播放会话，sorani第三季**第01集从接近0位置按实时速度播放至自然结束**，时长1,422,004毫秒（约23分42秒），随后自动进入**第02集并推进10,412毫秒**。结果同时确认实际设置和资料库已恢复。中途控制截图及自然下一集截图在本地证据目录。

该完整观看用例关闭弹幕，因此不能据它声明“整集弹幕同步通过”；它也不是第12集整集验收，更不是所有来源整集验收。其他播放器、弹幕、后台和恢复回归有各自证据，不混算为17源的真实观看结果。

## 下一步按阻断原因处理

1. **先完成剩余验证入口**：dalvdm、giriGiriLove电视端、mgnacg电视端；mutefun两设备和mgnacg现代环境已通过人工验证、LMM两设备已通过自动验证。限频与验证码分开提示，成功后必须恢复搜索/选集并实播；不要用无限重试解决限频。
2. **定位3个电视发现差异**：baimao、ezdmw、MXdm。先固定同一剧集/线路、规则、时间和请求上下文，再比较旧/新内核与原版；记录实际子资源加载/候选，不凭单条统计脚本语法错误下结论。
3. **复核HTTP与超时**：aafun／moonci失败线路、AGE、DM84及LMM补测404。保留状态码和请求阶段；错误不自动等于站点永久失效，也不自动等于客户端缺陷。
4. **版本化修订过时规则**：xfdmneo先核对站点真实搜索/详情/播放结构，再提规则更新；固定目录快照和用户规则不静默覆盖。
5. **扩大已通过来源的操作验收**：按2026-09-22用户调整，不再安排整集播放；改用代表来源短时播放、弹幕、拖动、换集与恢复专项。来源有一条成功线路，不掩盖同源其他线路失败。历史整集证据仍保留，但不作为新版退出条件。

原审计日志需要区分“按标题找到12集／按第12项回退／按第1集回退”，避免把索引回退误标为精准集数匹配。本表已按实际episode标题纠正统计，没有修改历史日志；主任务已移除索引11冒充12集的逻辑并改用集数解析，后续轮次按新逻辑记录。

原始逐事件JSONL、HTML、签名地址、Cookie及配置备份只保存在本地忽略目录，不随公开源码或发布包分发。相关实现与阶段证据见[执行记录](PLAYBACK-EXECUTION-20260920.md)及[上游规则对照](SOURCE-PARITY-20260920.md)。

后续补测原始记录：`LMM-tv-verified-final.txt`、`verification-LMM-tv-final.txt`、`verification-mutefun-modern-final.txt`。补测不代表主表其他失败已解决。

后续补测：`verification-mutefun-tv-final.txt`、`mutefun-tv-verified.txt`、`verification-mgn-modern-long.txt`、`mgn-modern-verified.txt`。截至这些补测，电视累计7来源10条不同线路短播成功；现代累计8来源17条不同线路短播成功（主轮与补测合并，非一次全目录运行）。随新解析修复进行的preview补测另记，不改写历史轮次。

## 0.3.2 Preview 补测

- `ezdmw-tv-preview.txt`：同第三季第12集，线路1和2完成全部短播检查；日志记录非零1920×1080网页尺寸及一次跨域播放器iframe回退。线路3仍探测失败。这是本次客户端通用修复的真实电视证据。
- `baimao-tv-preview.txt`：六条线路仍失败，语法错误只有host/line不足定位具体脚本，另采私有诊断。
- `MXdm-tv-preview.txt`：同第三季第12集，此次集表出现两条线路（早轮一条），两条仍未发现可用媒体，不能声称已修复。
- `verification-giri-modern-preview.txt`及`giri-modern-verified.txt`：图片人工验证后恢复4项搜索，线路1第12集、线路2因缺12集回退01均短播通过。

截至上述记录，合并历史补测的电视成功来源为8/17（12条成功线路），现代环境9/17（19条成功线路）；是多次样本证据汇总，不是所有节目可播放的比例，也不是一次全目录同时成功率。两环境及不同版本、抽样剧集不混作同条件对照。


### 暂停前最后回归

`baimao-tv-artplayer.txt`：最终候选在电视重新检查六条线路，本轮都找到第12集。线路1–5全部通过首帧、进度推进、拖动和暂停恢复；线路6媒体TLS连接失败。故障链已明确：Artplayer脚本新语法无法在66执行，但可见播放器iframe已包含明确媒体URL，通用参数提取并probe后可以播放，不需要替换网站脚本。

本表所列全目录轮与补测汇总为：电视9/17来源、17条成功线路；现代环境9/17来源、19条成功线路。数据随网站变化，本轮不是同一时刻全目录成功率，也不保证所有节目。最终`web-discovery-*-artplayer.txt`在两环境通过broken-bundle参数提取与隐藏/小尺寸广告排除；现代提前注入在旧provider明确SKIP。
