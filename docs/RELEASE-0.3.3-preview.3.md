# KazumiTV 0.3.3 Preview 3 候选

状态：源码提交和标签已推送，APK尚未公开发布。当前已验收候选内置开发者弹幕应用凭证，公开包发行配置待确认。

versionCode 49。在Preview 2基础上修复人工验证的等待与后台恢复、旧WebView的iframe媒体发现上下文，以及播放器初始焦点偶发崩溃。

- 播放器等待实际控件挂载后再请求焦点，替代固定两帧等待；切换页面取消旧请求，正常遥控移动不会因重新布局被抢回。
- 人工验证不再因等待60秒而停止检测；HOME退后台暂停检测，返回原页面继续，离开或重载仍会取消并释放。
- 兼容发现保留原嵌入页面；最多额外一个候选WebView，不用候选顶层导航覆盖原播放器。成功、取消和超时统一释放。
- 同一媒体地址按有效请求上下文去重，先到的错误Referer不会吞掉后到的正确请求；候选/探测总预算保持有界。
- 若候选页面仍需验证，在总等待结束时优先交回验证流程；同时继续等待原页面可能出现的媒体，页面离开验证后清除旧挑战状态。

189项单测、release构建和lint通过。播放器实际初始焦点和错误重试焦点、遥控器确认重试通过。最终APK电视受控回归覆盖延迟媒体、候选重定向、同URL错误上下文403后正确请求成功、候选验证错误优先级与过期挑战清除、取消释放，以及验证页长等待与后台返回。受控验证码使用固定测试输入，不代表真实网站人工验证已全部通过。

最终APK的真实baimao→sorani第12集换源通过：取消换源保持暂停，约32秒处切换，目标首次记录约38秒并推进至43秒；返回当前集高亮且目标来源历史正确。MXdm正式UI同词搜索返回0条，跨到该源的续播尚未通过，未把替代来源的通过覆盖此缺口。

较早code49候选的真实baimao第12集来源页/Dialog路径：暂停10秒后取消换线保持位置和暂停；播放约32秒后切到线路2，继承进度并推进至约43秒；返回原目录仍定位第12集。测试使用独立持久检查点，结束后设置、历史、搜索历史恢复且与原值相等。

最终APK的真实gugu3第01集解析、首帧、推进、拖动与暂停继续通过。17源完整矩阵仍保留本日code47结果；午间全目录复测因电视休眠中止，休眠期超时不计来源失败，详见[午间执行记录](PLAYBACK-MIDDAY-20260922.md)和[来源矩阵](SOURCE-VALIDATION-20260922.md)。giriGiriLove正确人工验证码后的原搜索恢复仍未验证；不做整集测试，不把短时播放或受控回归扩大为完整迁移完成。

APK SHA256：`f62680d3356ef05015b79b1e9f2624a1800ce0d14e3c11cbf75c79494ffed7c7`。包名与签名延续原生测试版，可覆盖安装；不包含用户数据、Cookie、私有页面或签名媒体地址。

另外已构建不内置弹幕凭证的本地候选，SHA256：`0516a45496bbf4e4284766ade89433be535dccbd7f14583f3815380ee2d98b38`。同一功能源码，仅发行配置不同；生成及编译字段已核对为空，但该配置APK尚未额外做真机验收或发布，不能套用上方APK校验值。
