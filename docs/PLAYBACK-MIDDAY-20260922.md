# 2026-09-22 午间播放优先续作

用户将工作窗口改为立即开始、14:00停止并收束；原14:00启动安排已删除，改为14:00停止提醒。完整迁移仍未完成，不做整集测试，不把首帧或受控测试当全部迁移完成。

基线：0.3.3-preview.2 / code48，提交93acef4397e84c05c034c71084ad1e8d854b674a。固定17条上游规则和现有主页方向保持，电视为原小米Android9/WebView66。用户设置、历史、搜索历史在本轮开始已做新的检查点；Quest3文档属于其他任务。

## 本轮具体缺口

1. 人工验证页的整体60秒超时停止了后续检测，迟到输入与网页验证完成可能不再跟进。可见页改为只在前台持续检测，退后台暂停、回前台恢复；保留单次JS调用超时、取消与WebView生命周期释放。自动验证的有限超时保持。
2. 旧内核兼容发现会用原WebView顶层打开iframe，丢失原嵌入上下文。gugu3当前播放器页面对顶层打开会重定向回来源，故不能把候选提升当原页面等价。兼容探测改为保留原页面、最多额外一个候选实例，总候选与超时预算仍有界。
3. 增加真实来源同集换线专项，检查暂停取消、同集进度、换后短推进和返回选集。实际结果按日志记录，不用合成媒体代替。

## 验证记录

- `artifacts/midday-20260922/user-data-backup.txt`：三份用户存储检查点保存。
- `giri-live.txt`：真实验证页面再次进入，但本次5分钟窗口内未收到人工验证码，正确验证码后的搜索恢复仍未验证；不是错误验证码提交结果，也不把超时算验证成功。

- `build49-awake-final.txt`：release、androidTest、189项单测（0失败/0错误）与lint通过。焦点修复前code49候选APK SHA256：`2bb1041186e43ca4f062a8a93e036e172259eaeaea18cd2e6d3bee6ce2bd2751`。较早`build49-final.txt`构建的`4a1eaca0…`仅属于下列标明的候选阶段，未混作最终APK。
- `verification-wait-lifecycle.txt`：候选阶段在真实电视等待62秒后仍检测；HOME后检测计数停止，回同一Activity/WebView继续；已知固定测试输入可提交并带Cookie恢复原搜索。这是受控验证页，不能替代真实网站验证码。
- `web-discovery49.txt`：较早code49候选电视兼容发现回归通过，包括保留原iframe的延迟媒体、独立候选重定向不破坏原发现、双实例取消释放、同一URL先错误Referer403后正确上下文成功；旧有无后缀/跨域Range/动态iframe/坏候选继续/广告和普通XHR噪声边界也通过。该电视不支持document-start，明确跳过，未声称现代提供者已复验。
- `baimao-road-switch.txt`：初版测试错误地观察隔离历史，但Compose Dialog实际使用自身Context。实际动画可见，测试等待历史超时；不作为应用换线通过或失败。`restored-after-road-harness.txt`已恢复本轮三份用户存储并核对相等。修正测试改用持久检查点保护的真实库，保留完整Dialog界面路径。

- `baimao-road-switch49.txt`：较早code49候选的实际来源页/Dialog路径通过：第12集暂停10秒→换线取消仍暂停10秒→遥控快进至32秒→线路1切2并推进至43秒→返回线路2当前第12集；三份用户存储恢复且相等。只证明该同来源同集样本。
- `gugu3-49.txt`：较早code49候选在真实gugu3仅11集目录回退抽第01集，媒体解析、首帧、实际推进、拖动、暂停继续全部通过。保留此前code47失败，记录本次修复后的样本增量，不改写旧轮统计。

## 休眠导致的无效测试轮

`full49.txt`原计划复查17条规则，但中途多条已解析媒体出现首帧超时。检查时电视为`mWakefulness=Asleep`、屏幕关闭，故这轮没有形成完整有效的17源矩阵；这些休眠期播放超时不计来源失败或版本回退。`power-asleep.txt`保留本机环境证据，休眠原因未确定。

已终止该轮，`restored-after-sleep.txt`确认三份用户存储恢复且相等，随后唤醒电视。测试播放器增加窗口保屏，源/线路边界、搜索/集表结果、播放等待及异常归类前检查`isInteractive`，休眠时明确取消审计，不更改电视全局电源设置。重新编译日志为`build-test-awake-guards.txt`。

## 焦点修复前候选复测（2bb10411…）

- `web-discovery49-final.txt`：旧内核兼容发现完整回归通过，并新增候选验证优先旧probe错误、原页面延迟媒体仍优先成功、候选正常导航后旧挑战清除三项。document-start明确因提供者不支持而跳过。
- `verification-wait-lifecycle49-final.txt`：等待62秒、网页操作模式无工具区遮挡、原生提交可用状态、HOME后计数3→3、同Activity/WebView回前台继续轮询、固定输入后Cookie和原搜索恢复全部通过。受控验证码，不代表真实giri正确验证码验收。
- `real-source-switch49-final.txt`：真实baimao第12集已播放，取消换源后保持10秒暂停通过；实际换到MXdm时搜索为空，等待结果超时，未进入跨源播放。检查点已恢复且三份存储相等。这次失败原因尚未确定，不写成跨源进度已通过或站点失效；测试增加输入回读/主线程等待和实际搜索关键词匹配布尔诊断后再试。

- `recent-watch49-final.txt`：最近观看、番剧详情、原来源选集入口受控导航回归通过。
- `release-bundle49-final.txt`：release不可调试，内置弹幕配置可完成实际映射并取得7310条评论；不输出应用凭证。
- `gugu3-49-awake-final.txt`、`akianime-49-awake-final.txt`、`baimao-49-awake-final.txt`：唤醒后的3源定向检查完成，gugu3第01集、akianime线1回退01、baimao线1–5第12集均通过完整短测；akianime线2发现失败、baimao线6媒体探测失败。不会将不同APK抽样相加写成完整新矩阵。

### 跨源复试发现的焦点崩溃

`real-source-switch49-retry.txt`已补输入确认，但在进入首个播放器时进程崩溃，尚未运行到第二次搜索。13:32:50的AndroidRuntime栈定位`PlayerScreen.kt`焦点请求：延迟两帧仍可能早于目标挂载，`FocusRequester is not initialized`。不与10:08旧Parcel崩溃混同，也不将该轮写成已验证跨源成功。该轮因进程退出未执行测试finally，需用开始前持久检查点恢复用户数据。

`restored-after-focus-crash.txt`已用午间开始前检查点恢复并核对三份用户存储相等。生产修复将请求绑定实际控件的布局附着；卸载取消，成功后不因重布局抢焦点。新增回归要求播放按钮真正获焦、失败后重试按钮获焦并由遥控器确认键实际重试。

## 焦点修复后的发行候选

`build49-focus-final.txt`：release、androidTest、189项单测和lint通过。发行候选APK SHA256：`f62680d3356ef05015b79b1e9f2624a1800ce0d14e3c11cbf75c79494ffed7c7`，versionCode49 / 0.3.3-preview.3。此前2bb10411候选的媒体发现、人工验证实现未改动；新增播放器焦点挂载修复。

- `player-controls49-focus-final.txt`：控制栏/设置/菜单/返回及原有错误恢复回归通过，并确认初始播放按钮实际获焦、失败后重试按钮实际获焦，遥控器确认触发一次重试。
- `real-source-switch49-focus-final.txt`：真实baimao初始播放、暂停及取消换源保持10秒均通过，没有重现焦点崩溃；但MXdm的正式UI搜索在日志明确`expected_keyword=true`时仍返回0条，随后等结果超时。预查询能找到同关键词第三季，本次差异原因未定，不归因于测试输入时序，也不算跨源迁移已通过。三份用户存储已恢复且相等。
- `real-source-switch-sorani49-final.txt`：最终APK真实baimao→sorani第12集通过。取消换源保持10秒暂停；遥控快进后32秒换源，目标首次历史位置37941ms，继续至43秒；返回后当前第12集高亮，历史来源为sorani线路1。仅此两源同集样本，不验证不同片头时长或返回后再次入播；真实UI只配置这两条真实规则，搜索/集表/解析/视频均来自实际站点。持久检查点恢复三份存储并核对相等。
- `web-discovery49-focus-final.txt`：最终APK再次通过全部兼容发现/候选挑战/请求上下文/取消释放回归；document-start仍为不支持而跳过。
- `verification49-focus-final.txt`：最终APK等待62秒后仍可完成受控验证；HOME计数4→4、同Activity/WebView返回继续、网页输入/原生按钮状态与Cookie原搜索恢复均通过。
- `gugu3-49-focus-final.txt`：最终APK真实gugu3第01集解析、首帧、实际推进、拖动和暂停继续全部通过。

## 收束与剩余项

13:45前结束实现和真机测试。`restored-final.txt`确认设置、历史、搜索历史恢复到午间开始前检查点且相等；电视已从正常启动器回到原主页，安装版本49 / 0.3.3-preview.3，未清数据、来源、Cookie或改变全局电源设置。之后只整理发行材料和核对发布，14:00为停止上限，不重新启动目标。

本轮已验收候选为`f62680d3…`最终APK；完整迁移仍未完成：giri正确人工验证码与原请求恢复、MXdm正式跨源UI同词零结果、其余来源表内HTTP/集表/验证和解析失败仍待处理，现代WebView分支未本轮重验，不同片头时长、待机/网络变化和其余台账能力也未关闭。后续继续采用短时真实播放与操作/恢复专项，不做整集测试。

### 发布受阻

源码提交`fc0f32824ebfe976d662c99451ad5da4ab2e5c7e`和标签`v0.3.3-preview.3`已推送。上传APK前自动审批拒绝执行：当前包含私有弹幕API凭证，现有发布授权没有明确涵盖公开这些凭证。因此APK未上传、Release未创建，不能称已发布。已请求用户选择不内置凭证的公开包或明确允许现包公开；无论是否收到回复，14:00仍停止。README暂保留Preview 2的有效公开下载入口，源码功能修改保留。
