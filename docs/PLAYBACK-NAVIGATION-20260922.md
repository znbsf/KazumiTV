# 已看番剧入口与验证界面续作

用户补充要求：逐源和验证码结果需要分清；验证UI不能遮挡操作；看过的番剧应有直接返回番剧、选集和续播的选项。保持既有主页方向与用户数据，不做整集测试。

## 实际缺口与改动

原首页普通海报进入详情后只提供搜索来源，详情没有读取该作品历史；历史记录只能直接播放，缺少明确的番剧详情动作。数字入口反而自动续播，与海报不一致。

- 原首页64dp标题区加入最多两条按作品去重的最近观看链接，保持六列、两排海报及网格高度。
- 作品详情显示该作品最新记录的继续播放、原来源选集和更换来源；历史条目增加番剧详情。
- 查看原来源选集使用保存的来源、目录URL、线路名和集URL定位，不触发媒体解析或自动播放，也不先全源搜索。旧记录缺目录或来源被移除时给出说明并回退。
- 历史更新时入口刷新；数字入口与普通海报统一进入详情。返回恢复最近观看或对应详情操作的焦点。
- 验证页工具栏可换行；“操作网页”收起顶部工具区，仅保留一行操作说明，扩大网页可操作区域；Back恢复工具栏和焦点。输入区增加键盘避让与明确无障碍名称。

## 来源与验证码口径

[17源短测表](SOURCE-VALIDATION-20260922.md)已经逐条记录：7源14线短测通过，并不是17源都能播放。5源停在验证阶段，3源在集表/媒体阶段失败，DM84搜索HTTP522，xfdmneo搜索为空；失败线路不能被同源成功线路掩盖。

本次真实验证追加检查（时间与前轮不同）：

| 来源 | 已观察事实 | 仍未通过的部分 |
|---|---|---|
| LMM | 原公开code47验证UI完成，并恢复原搜索5项 | 不据此增加最终code48播放线路通过数 |
| mutefun | code47实际验证码图片、输入与提交页面可见 | 本轮未完成人工输入后的真实提交；探索运行被后续测试中止，不作为客户端崩溃证据 |
| giriGiriLove | code47实际验证码图片、输入及提交页面可见 | 等待窗口内无人输入，真实提交/搜索恢复未验 |
| mgnacg | code47进入实际验证界面 | 等待窗口内无人输入，真实提交/搜索恢复未验 |
| dalvdm | 本次原搜索请求超时，未到验证页；前轮记录页面无图片、XPath不符 | 验证仍未关闭，不能把超时当成已修或假验证成功 |

本地固定验证码测试只验证应用布局、输入提交、Cookie和原请求恢复，不能替代上述真实站点人工验证结果。原始截图、私有DOM及数据检查点保存在忽略目录 `artifacts/verification-navigation-20260922`，不提交验证码页面、Cookie或用户数据。

## code48 验证

- `build48-final.txt`：release、androidTest、188项单测及lint通过；随后补来源列表“当前来源/切换后搜索”说明并修订测试诊断/焦点断言，`build48-label.txt`重新构建release/androidTest和lint通过。发行APK以Preview 2校验值为准。
- `recent-watch-navigation-48-verified.txt`：生产组件配隔离数据/受控来源，最近入口→详情、续播参数、原来源集表定位、无自动解析/无全源搜索、历史详情、记录更新与去重、隐身和来源移除回退通过。
- `verification-layout-48-verified.txt`：图像/输入/提交控件均在屏内且彼此/网页不重叠；网页模式扩大区域、真实Back恢复工具栏及焦点；已知测试输入提交，带Cookie恢复原搜索通过。
- `verification-48-verified.txt`：按钮/脚本/图片提交、假成功防护、取消、原POST和Cookie重试通过。
- 最终只改来源说明文字的发行APK，再跑`verification-layout-48-published.txt`与`recent-watch-navigation-48-published.txt`均通过。
- 初期布局测试错误地只检查按钮内文字节点焦点；设备诊断证明按钮父节点已获焦，修正检查后通过。保留失败日志，不将该测试断言错误描述为真实站点已修复。

以上受控测试不证明所有真实验证码都能通过，也未重复整集测试。

正常应用入口另有实际数据验收：主页最近观看可遥控聚焦并进入原番剧详情，显示继续第13集与原来源选集；点击选集读取baimao原目录，六条线路、13项集数，定位第13集且未自动播放。两次Back回详情后焦点位于原来源选集按钮，再Back回首页原最近观看链接。截图/XML分别为`recent-home`、`recent-detail-final`、`recent-episodes-loaded`、`detail-back-focus`及`recent-return-focus`，均只存本地。

最终code48 giri真实验证页面可见，但本轮未完成人工输入；等待后由主代理结束该次测试，工具日志的Process crashed为主动停止，并非证明应用自行崩溃。仍列真实提交待验，不沿用旧截图提交验证码。

最终发行APK正常详情“继续第13集”短测：从既有00:29续播至00:34，实际画面及6140条匹配弹幕可见（`detail-real-resume.png`），随后结束测试。`user-data-restored-final.txt`确认设置、历史、搜索历史三份检查点恢复且逐项相等，电视返回正常首页。没有执行整集播放。
