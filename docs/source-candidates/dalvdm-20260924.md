# dalvdm 独立候选规则 · 2026-09-24

候选文件：[dalvdm-20260924.json](dalvdm-20260924.json)。这是与固定 KazumiRules `0d85fc80ab6c208548d9ee9c9e81271b08ff7f39` 快照隔离的测试输入；没有写入用户 RuleStore，也没有改动公开规则或生产 APK。

## 现场与改动

生产 APK `9EFBBEFC…` 上，实时验证码图由本轮截图识别并填写。提交后挑战消失，搜索页 HTTP 200、页面标题为“无职转生搜索结果”，但原规则的 `searchList=//div[@class='vod-detail style-detail cor4 search-list']` 匹配 0 个。页面实际包含 `main.search` 内的详情链接。第一版候选搜索返回 6 项，但原集表选择器为 0；从真实详情页更新集表为 `div.tab-pane.lists-box` 内的播放链接。候选文件仅调整名称、版本、搜索与集表 XPath，保留原站点地址、请求和验证配置。

测试 APK `77C4F0137061C3445C49840BC92F49129FA8C184AD3CAC004974393B4E9CCAFC` 的 `candidate-rule` 校验通过：生产解析器接受此 JSON、隔离仓库生效、未导入用户规则、9 项路径边界检查通过。真实候选搜索 6 项，目标第三季详情 3 线、每线 13 集。第 12 集逐线抽样：1 线 HLS 已解析但播放器错误码 2004；2 线首帧成功但播放推进超时；3 线媒体探测 HTTP 404。**0/3 线完成短播标准，候选尚不可发布。**

网页、完整请求、媒体地址及截图仅在被忽略的 `artifacts/p1-source-snapshot/dalvdm-*` 与设备应用目录；版本化文档仅保留去敏后的现象。下一步需分别诊断 1 线播放器错误和 2 线推进，不能把搜索/集表修复等同于播放修复。固定 17 源统计仍记 `dalvdm` 未进入集表。
