# xfdmneo 独立候选规则 · 2026-09-22

状态：仅公开页面与结构审查，未完成人工验证、搜索/集表或播放验收。不是固定17源快照的更新，也未导入电视。候选文件：[xfdmneo-20260922.json](xfdmneo-20260922.json)。

## 来源依据

- 固定 KazumiRules 提交 `0d85fc80ab6c208548d9ee9c9e81271b08ff7f39` 的 xfdmneo 版本1.1，来源矩阵记录的规范化规则SHA256 `351ad39aa999ec9c0b0ba6e28f79a6de93dd0267883fd3ad8db00274e92bada1`。本次查阅的[公开上游规则](https://raw.githubusercontent.com/Predidit/KazumiRules/main/xfdmneo.json)仍使用旧域名，不能把本候选说成上游已发布更新。
- 2026-09-20已保存的旧域名搜索响应重定向到综合门户，正文明确提供“旧版 anime.xifanacg.com”和“新版 Next”入口。该本地私有诊断不随本说明复制；候选域名来自页面公开指引，不是猜测替换域名。
- 2026-09-22无Cookie读取[旧版站首页](https://anime.xifanacg.com/)得到HTTP200及动漫目录，页面表单是GET `/search.html`。
- 同日按该表单搜索“无职转生”得到HTTP200的DS验证码页：`input[type=text][name=verify].ds-verify`、`img.ds-verify-img`、`button.verify-submit`。普通页面标题不包含安全验证词，因此不能依赖标题识别。未读取或识别验证码图片，未提交验证码。

## 具体差异与限制

1. 名称改为`xfdmneo-candidate-20260922`、版本为`1.1-tv-candidate.1`，避免按名称导入时覆盖原规则。
2. `baseURL`与`searchURL`使用公开旧版入口；保持GET方法与原搜索路径。
3. 新增现有schema支持的`antiCrawlerConfig`：enabled=true、captchaType=1、captchaDetectType=1；XPath按已观察到的元素类型、name和class token选择。没有验证码脚本、接口构造或自动提交。
4. 搜索、详情、线路XPath与原规则一致，**仅作为待核对候选保留**。验证码页不能证明通过验证后的列表仍兼容。
5. 保留原媒体与legacy解析字段；尚无本候选解析或播放通过证据。通用DS兼容层仅在其自身严格条件成立时适用，此文件不放宽条件。

## 必须完成的门槛

- 用生产导入器检查结构，并在隔离测试配置中使用候选；保留原快照与用户规则，不自动启用或覆盖。
- 人工完成网站正常验证码，并确认原搜索请求恢复；网页出现图片或控件不等于验证通过。
- 检查实际搜索结果的标题、详情地址与季度；再检查详情线路和集数。若XPath不兼容，基于实际响应修订候选版本，不能把门户或推荐链接当作搜索结果。
- 进入真实节目，验收媒体解析、首帧、持续推进、拖动、暂停继续和返回恢复；记录具体APK、规则版本、来源/线路/集数及失败阶段。
- 只有上述步骤通过后才讨论推广或向上游提交。局部短测不能代表全站、所有线路或完整迁移通过。

本次仅进行了JSON、字段、URI及9条XPath的静态语法检查（.NET XPath编译，不是Android引擎执行），未执行Gradle构建、仓库JVM单测或设备验收。

后续增量：Preview4现代TV模拟器通过 `candidate-rule`，使用生产 `RuleImport.parse` / `validateImport` 读取本候选，9个路径/名称/多规则负例被拒绝。候选仅位于应用外部文件目录，不写RuleStore；实际验证入口支持显式candidateFile参数。此增量仍未访问网络或证明人工验证、XPath匹配及播放通过。

真实旧电视增量（本地code50配置包 `bb07e865…`）：首次WebView加载遇限频页，现有UI显示限频提示；正常重新加载后出现原生图片验证码及提交控件，旧DS兼容条件成立。5分钟内未收到人工输入，测试等待结束，未提交、未确认验证成功、未验证搜索或播放。延迟回复不能用于已结束的页面。

后续诊断增量（本地候选 `bd160a76…`，不替换已发布APK）：候选仓库直接使用临时规则，避免默认读取RuleStore时可能触发损坏规则修复。现代模拟器结构检查证明访问tv_rules即失败的哨兵未被触发，实际用户规则前后相等；候选source-audit结果明确candidate=true/fixedSnapshotTested=false、loaded_without_import，真实搜索返回needs_manual_verification。没有冒充固定17源成功结果。

2026-09-24 静态对照：官方入口另提供 Next 站。带关键词的 `/search?q=…` 返回 HTTP 200，但初始 HTML 没有本候选的 `search-box` 节目节点；其公开客户端脚本使用 `search_animes` 和 `has_gated_anime_search_match` JSON RPC。Next 站并非将旧版页面换域名，当前 XPath 规则不能直接复用。没有调用该 RPC，也没有将 Next 站逻辑混入本候选；本候选仍须人工完成旧版站图片验证，再核对搜索、详情、集表与播放。取证摘要见[P1 矩阵](../PLAYBACK-P1-MATRIX-20260924.md)。
